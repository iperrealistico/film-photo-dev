# Reference Docs

This section keeps tracked reference material that is useful context but should not crowd the top-level `docs/` surface.

## Subdirectories
- `original-request/`
  - original product request, PRD, architecture proposal, or initial plan.
- `historical/`
  - progress trackers, earlier strategies, old analyses, or preserved snapshots.

## Current State
- [Verified] The tracked reference sources currently include the initial product brief and a historical feature inventory extracted from two user-provided legacy single-page apps.
- [Verified] The `historical/` directory is now used for preserved analyses of legacy material that inform the rebuild without defining present-day implementation truth.

## Reading Rule
Treat these files as historical or source material, not automatic present-day truth.
Prefer:
- `../system-overview.md`
- current code and config when they exist
- the local-only dossier

when evaluating the current state.
