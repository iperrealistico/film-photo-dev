# Film Dev QA Report

Date: 2026-04-18

## Summary

Status: PASS with residual risks

The app passed the automated test suite, the production-build browser sweep, the offline reload check, and the hidden-log/logic-trace review performed for this session.

I did not find any app-thrown browser errors, silent calculation mismatches, or hidden-log contradictions in the flows exercised here.

## Test Environment

- Workspace: `film-dev`
- Platform: macOS desktop
- Browser: Headless Chrome via Playwright CLI
- App target: local production preview served from `vite preview`
- Preview URL: `http://127.0.0.1:4173/`
- Date/time zone: `2026-04-18`, `Europe/Rome`

## Commands Run

```bash
npm run qa
npm run preview -- --host 127.0.0.1 --port 4173
```

Browser automation and artifacts were collected under:

- `output/playwright/full-qa-2026-04-18/.playwright-cli/`

## Automated Verification

`npm run qa` passed.

What it covered:

- `npm test`
- `npm run build`

Observed result:

- 9 test files passed
- 39 tests passed
- Production build completed successfully

Build output summary:

- `dist/index.html`
- `dist/assets/index-D8WGN5uk.css`
- `dist/assets/index-B-lwdc4o.js`

## Browser Feature Sweep

### 1. Recipe Browser

Verified:

- initial recipe grid loads correctly
- 3-up recipe presentation works
- bottom guidance/hero content renders
- search works
- empty state appears when there are no matching recipes

Observed result:

- search for `zzz` returned `No recipes found`
- clearing the search restored all 3 recipes

### 2. Cs41 Flow

Tested setup inputs:

- push/pull selection
- reused developer mode
- prior-roll count

Tested review output:

- developer base time
- push factor
- reuse factor
- final developer time
- fixed blix time
- full phase list
- warnings

Observed result:

- base time: `3.50 min`
- push adjustment: `1.75x`
- reuse adjustment: `1.06x`
- final developer time: `6.50 min`
- blix: `8.00 min`

The arithmetic shown by the UI and the hidden `calculationTrace` matched:

- `3.50 * 1.75 * 1.06 = 6.49`, rounded/displayed as `6.50 min`

Session runtime checks:

- session started correctly
- pause worked
- paused countdown stayed visually frozen
- completion view rendered
- chemistry-log save path worked

### 3. HC-110 Flow

Tested setup inputs:

- film format and quantity
- total working volume
- dilution math

Tested review output:

- film area conversion
- syrup/water split
- minimum active-agent threshold
- danger warning
- stronger-dilution recommendation
- minimum-volume recommendation

Observed result for the exercised case:

- load: `2 35mm rolls`
- film area: `160.0 in²`
- total working volume: `350.0 ml`
- syrup in mix: `10.9 ml`
- water in mix: `339.1 ml`
- minimum syrup needed: `12.5 ml`
- status: `danger`
- stronger dilution recommendation: `A · 1+15`
- minimum volume at current dilution: `400 ml`
- max safe load at this mix: `1 35mm roll`

The values were internally consistent with the logged formula:

- area model: `2 * 80 in² = 160 in²`
- minimum active agent: `(160 / 80) * 6.25 = 12.5 ml`
- working-solution split at `1+31`, `350 ml total`: `350 / 32 = 10.94 ml concentrate`

The planner trace recorded the full decision path, including:

- base time source
- temperature compensation
- agitation rule
- film area
- mix calculation
- capacity threshold
- final danger result

### 4. Df96 Flow

Tested:

- setup screen
- review plan
- source-backed minimum handling

Observed result for default exercised case:

- temperature: `80°F`
- recommended minimum: `3.00 min`
- user-entered monobath time: `4.00 min`
- final monobath time: `4.00 min`
- wash time: `5.00 min`

The logged trace correctly explained the behavior:

- temperature selected the `constant zone`
- the user-entered monobath time was retained because it exceeded the minimum

### 5. Mix Workspace

All four calculator modes were opened and exercised.

#### Scale Kit

Observed result:

- bottle size: `1000 ml`
- full bottle makes: `5 L`
- target batch: `500 ml`
- output: `100.0 ml concentrate + 400.0 ml water`

#### Ratio

Observed result:

- ratio: `1+31`
- final batch: `500 ml`
- output: `15.6 ml concentrate + 484.4 ml water`

#### Multi-Part

Observed result:

- reference recipe: `1000 ml`
- parts: `25 ml A + 25 ml B`
- target batch: `500 ml`
- output: `12.5 ml A + 12.5 ml B + 475.0 ml water`

#### On Hand

Observed result:

- ratio: `1+31`
- concentrate on hand: `18.0 ml`
- output: `558.0 ml water`
- maximum working volume: `576.0 ml`

### 6. Saved Presets

Verified:

- preset save from plan review
- preset appears in Saved screen

Observed result:

- saved preset label: `HC-110 · 22:20`

### 7. Chemistry Batches

Verified:

- chemistry log created from completed-session summary
- entry appears in Batches screen

Observed result:

- batch visible for `CineStill Cs41`

### 8. Settings, Diagnostics, and Exports

Verified:

- theme mode selection
- left/right-handed layout toggle
- export presets
- export chemistry logs
- export all local data
- hidden diagnostics visibility
- breadcrumb creation
- diagnostics bundle rendering

Observed result:

- preferences persisted to local storage
- theme changed to `standard`
- layout changed to `leftHanded: true`
- all three export actions produced JSON downloads successfully
- breadcrumb insertion immediately appeared in the hidden debug log

Export files generated:

- `output/playwright/full-qa-2026-04-18/.playwright-cli/film-dev-saved-templates-2026-04-18T11-23-30-078Z.json`
- `output/playwright/full-qa-2026-04-18/.playwright-cli/film-dev-chemistry-logs-2026-04-18T11-23-31-904Z.json`
- `output/playwright/full-qa-2026-04-18/.playwright-cli/film-dev-local-data-2026-04-18T11-23-33-770Z.json`

Payload sanity checks passed:

- `saved_templates` export included `presets`
- `chemistry_logs` export included `batches`
- `all_local_data` export included `preferences`, `presets`, `batches`, `activePlan`, `activeSession`

## PWA And Offline Verification

Static endpoint checks passed:

- `GET /` -> `200`
- `GET /manifest.webmanifest` -> `200`
- `GET /sw.js` -> `200`

Service-worker/browser evidence:

- debug log recorded `service_worker_registered`
- scope recorded as `http://127.0.0.1:4173/`

Offline check:

- browser forced offline
- page reloaded successfully while offline
- recipe browser still rendered correctly
- no browser console warnings or errors were emitted during the offline reload

## Browser Console Review

Result:

- browser console errors: `0`
- browser console warnings: `0`

This held across:

- normal production-load flow
- feature sweep
- export flow
- offline reload

## Hidden Debug Log Review

IndexedDB debug log summary at the end of the sweep:

- total entries: `172`
- error entries: `0`
- warning entries: `1`

The single warning was expected and intentional:

- `lifecycle · browser_offline`

Important categories present in the log:

- `planner`
- `runtime`
- `storage`
- `ui`
- `pwa`
- `lifecycle`

Important verified entries:

- `planner · plan_review_requested` for Cs41
- `planner · plan_review_requested` for HC-110
- `planner · plan_review_requested` for Df96
- `storage · preset_saved`
- `storage · batch_saved`
- `diagnostics · manual_breadcrumb_recorded`
- `pwa · service_worker_registered`

This confirms that the app is not only computing results, but also leaving a usable forensic trail for support and debugging.

## Logic Review Verdict

### Cs41

Verdict: internally consistent in the exercised case

Why:

- displayed developer time matched the traced base time, push factor, and reuse factor
- blix remained fixed under reuse, matching the app’s stated rule
- review warnings matched the chosen inputs

### HC-110

Verdict: internally consistent in the exercised danger case

Why:

- film-area conversion matched the selected load
- minimum-concentrate threshold matched the logged rule
- syrup/water split matched the dilution math
- danger outcome and rescue suggestions matched the computed shortfall

### Df96

Verdict: internally consistent in the exercised minimum-floor case

Why:

- minimum time was derived from the temperature zone
- longer user input was preserved above the minimum
- review warnings correctly described that choice

## Findings

No app defects were discovered in the features exercised during this QA run.

## Residual Risks

These are the main remaining trust limits after this pass:

1. Mobile-device-specific behavior still needs real hardware verification.
   This run used desktop Chromium, not iPhone Safari or Android Chrome.

2. PWA install UX was not physically exercised on iOS/Android.
   Offline reload passed in desktop Chromium, which is a strong sign, but not the same as full Add-to-Home-Screen/mobile lifecycle testing.

3. Chemistry correctness outside the exercised combinations still depends on the bundled recipe set.
   The tested logic was internally consistent, but this does not magically validate every future recipe combination.

4. Audio/vibration delivery was not validated on hardware in this session.

## Final Verdict

For the scope tested here, the app is behaving like a trustworthy offline-first darkroom tool:

- automated tests passed
- production build passed
- primary user flows worked
- offline reload worked
- exports worked
- browser console stayed clean
- hidden diagnostics matched the visible behavior

The next highest-value QA step is real-device validation on iPhone and Android, especially for:

- PWA install flow
- backgrounding
- wake/sleep recovery
- cue audio/vibration
- one-handed darkroom use under real lighting conditions
